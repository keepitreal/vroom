---
"react-native-vroom-chart": minor
---

Add bridgeless mode support for React Native 0.85+

Implements RCTTurboModuleWithJSIBindings (iOS) and TurboModuleWithJSIBindings (Android) to install JSI bindings via the new bridgeless architecture. The legacy installation path remains for backwards compatibility with older RN versions.
